# =====================================================
# AI-Hub 测试环境配置模板
# 复制为 .env.test 后按需修改，.env.test 不提交到 Git
# 说明：修改 app/core/config.py 中 IS_TEST = True 即可使用此配置
# =====================================================

# .NET 后端服务地址
DOTNET_BASE_URL=http://localhost:5000

# 内部 API 鉴权 Token（需与 .NET 后端的 InternalToken 一致）
INTERNAL_TOKEN=test-token-123

# 默认租户 ID（与 ai-hub-ai 测试数据一致）
DEFAULT_TENANT=default_test

# ========== 审计日志 ==========
ENABLE_AUDIT_LOG=true

# 附件文件基础路径（本地目录，用于文件匹配）
ATTACHMENT_BASE_PATH=D:\path\to\your\attachment\folder
ATTACHMENT_BASE_URL=http://localhost:5000/uploads_test

# ========== 对话 LLM（百炼兼容）==========
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=deepseek-v3.2

# DeepSeek 直连备用（未配置 LLM_BASE_URL 时生效）
DEEPSEEK_API_KEY=sk-your-deepseek-api-key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# ========== SQL Server（kb_article 来源，仅 SELECT）==========
# 需与 ai-hub-service Program.cs 连接同一服务器
SQLSERVER_DSN=Driver={ODBC Driver 17 for SQL Server};Server=172.16.15.9;Database=ai_hub_test;UID=sa;PWD=your-password;TrustServerCertificate=yes;
KB_SQLSERVER_CONNECTION_STRING=Driver={ODBC Driver 17 for SQL Server};Server=172.16.15.9;Database=ai_hub_test;UID=sa;PWD=your-password;TrustServerCertificate=yes;

# ========== Chroma 向量库 ==========
CHROMA_PERSIST_DIR=./data/chroma_test
CHROMA_COLLECTION=kb_articles_test
CHROMA_PERSIST_PATH=./data/chroma_test

# ========== Embedding（百炼 text-embedding-v3）==========
EMBEDDING_PROVIDER=openai
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_EMBEDDING_MODEL=text-embedding-v3
DASHSCOPE_API_KEY=sk-your-dashscope-api-key
