# .NET 后端服务地址
#DOTNET_BASE_URL=http://localhost:5000
DOTNET_BASE_URL=http://localhost:5000

# 内部 API 鉴权 Token（需与 .NET 后端的 appsettings.json 中的 InternalToken 一致）
INTERNAL_TOKEN=your-internal-token-change-in-production

# 默认租户 ID（可选，缺省为 "default"）
DEFAULT_TENANT=default

# 附件：二选一 —— 本地目录 或 远程 api/files/list
# 方式一：本地目录（开发用）
# Windows 示例：D:\01-资料\永红造型线维修视频
ATTACHMENT_BASE_PATH=
# 附件访问 URL，示例：http://localhost:5000/uploads
ATTACHMENT_BASE_URL=http://localhost:5000/uploads

# 方式二：服务器附件 API（生产用，配置后优先于本地目录）
# 列表接口：GET {ATTACHMENT_FILES_API_BASE_URL}/api/files/list?path={ATTACHMENT_REMOTE_PATH}
# 示例：https://www.yonghongjituan.com:4023
# ATTACHMENT_FILES_API_BASE_URL=https://www.yonghongjituan.com:4023
# 远程路径，对应服务器上的文件夹，如：diyi/永红造型线维修视频
# ATTACHMENT_REMOTE_PATH=diyi/永红造型线维修视频
ATTACHMENT_FILES_API_BASE_URL=
ATTACHMENT_REMOTE_PATH=

# 对话 LLM（二选一）：
# 方式一：百炼兼容（与示例一致，openai 库 + base_url + model）
# 配置后对话、意图分类、兜底回答均走百炼，可用 deepseek-v3.2 / qwen-plus 等
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=deepseek-v3.2
# API Key 用 DASHSCOPE_API_KEY（见下方），或单独设 LLM_API_KEY=
# 方式二：DeepSeek 直连（不配 LLM_BASE_URL 时生效）
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# ========== 向量能力闭环：从 SQL Server 读 kb_article → 拆分 → embedding → Chroma ==========
# SQL Server（kb_article 来源，仅 SELECT）
SQLSERVER_DSN=Driver={ODBC Driver 17 for SQL Server};Server=localhost;Database=ai_hub;Trusted_Connection=yes;
# 也可以用用户名密码：
# SQLSERVER_DSN=Driver={ODBC Driver 17 for SQL Server};Server=localhost;Database=ai_hub;UID=sa;PWD=xxx;
# 兼容旧配置名
KB_SQLSERVER_CONNECTION_STRING=Driver={ODBC Driver 17 for SQL Server};Server=localhost;Database=ai_hub;Trusted_Connection=yes;

# Chroma（向量库）
CHROMA_PERSIST_DIR=./data/chroma
CHROMA_COLLECTION=kb_articles
# 兼容旧配置名
CHROMA_PERSIST_PATH=./data/chroma

# Embedding：fake | openai（语义向量，支持 OpenAI / DeepSeek / 阿里百炼 DashScope）
EMBEDDING_PROVIDER=fake
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# 百炼 text-embedding-v3 可选向量维度（不填用模型默认 1024）
# OPENAI_EMBEDDING_DIMENSIONS=1024
# 阿里百炼（用 text-embedding-v3 时填 base_url + 模型 + DASHSCOPE_API_KEY）
# OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# OPENAI_EMBEDDING_MODEL=text-embedding-v3
DASHSCOPE_API_KEY=
# 兼容旧配置名
EMBEDDING_API_KEY=
EMBEDDING_BASE_URL=https://api.openai.com/v1
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIM=1536
