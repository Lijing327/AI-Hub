# ========== 生产环境配置示例 ==========
# 复制此文件为 .env.production，然后填写实际值
#
# 前端部署地址示例（同域名不同子路径）:
#   - 智能客服: https://your-domain.com/cs/
#   - 学习/知识库: https://your-domain.com/learning

# .NET 后端服务地址（内网地址）
DOTNET_BASE_URL=http://127.0.0.1:5000

# 内部 API 鉴权 Token（与 .NET 后端的 InternalToken 一致）
INTERNAL_TOKEN=your-secure-random-token

# 默认租户 ID（智能客服 /cs 不区分租户时此配置仅影响其它接口）
DEFAULT_TENANT=default

# ========== 审计日志 ==========
ENABLE_AUDIT_LOG=true

# 附件文件基础路径（服务器上的实际目录）
ATTACHMENT_BASE_PATH=/path/to/attachments

# 附件文件访问 URL（生产域名）
ATTACHMENT_BASE_URL=https://your-domain.com/uploads

# ========== SQL Server ==========
SQLSERVER_DSN=Driver={ODBC Driver 17 for SQL Server};Server=数据库IP;Database=ai_hub;UID=用户名;PWD=密码;TrustServerCertificate=yes;

# ========== Chroma 向量库 ==========
CHROMA_PERSIST_DIR=./data/chroma
CHROMA_COLLECTION=kb_articles
CHROMA_PERSIST_PATH=./data/chroma

# ========== Embedding ==========
EMBEDDING_PROVIDER=openai
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_EMBEDDING_MODEL=text-embedding-v3
DASHSCOPE_API_KEY=your-api-key

# ========== 对话 LLM ==========
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=deepseek-v3.2

# DeepSeek 直连备用
DEEPSEEK_API_KEY=your-deepseek-api-key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
