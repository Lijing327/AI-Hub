# 本地开发环境（npm run dev 时自动生效）
# 留空则使用相对路径 /api、/python-api，由 Vite proxy 代理到 localhost:5000 / localhost:8000
# 与 after-sales-ai 一致：dev 用代理，build 用 .env.production 中的完整地址

VITE_API_BASE=
VITE_PYTHON_BASE=
VITE_INTERNAL_TOKEN=test-token-123
VITE_DEFAULT_TENANT=default
