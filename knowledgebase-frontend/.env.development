# 本地开发环境配置
# 通过 Vite proxy 代理到本地后端服务

# API 地址（开发环境通过 vite.config.ts 中的 proxy 代理）
VITE_API_BASE_URL=/api

# Python AI 服务地址
VITE_PYTHON_API_BASE_URL=/python-api

# 内部 Token（与后端配置一致）
VITE_INTERNAL_TOKEN=test-token-123

# 默认租户
VITE_DEFAULT_TENANT=default
